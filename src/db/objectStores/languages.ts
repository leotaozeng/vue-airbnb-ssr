import TypeObjectStore from '../type';

const webLang = {
  keyPath: 'id',
  indexs: ['name']
};

const languagesObjectStore: TypeObjectStore = {
  languages: webLang
};

export default languagesObjectStore;
